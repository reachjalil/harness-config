interface:
  display_name: "Harness config"
  short_description: "Adopt portable Harness config resource groups and activation."
  default_prompt: "Use $harness-config to assess this repo, perform a clean full Harness config install when existing agent surfaces are present, and validate activation with harnessconfig.dev as the reference."
