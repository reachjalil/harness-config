interface:
  display_name: "Harness config Specs"
  short_description: "Navigate the Harness config v1 standard"
  default_prompt: "Use $harness-config-specs to update the Harness config standard and conformance docs for a new projection behavior."
