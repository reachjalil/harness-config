interface:
  display_name: "Harness config Patterns"
  short_description: "Apply Harness config implementation patterns"
  default_prompt: "Use $harness-config-patterns to design a Harness config projection implementation change that fits this repository."
