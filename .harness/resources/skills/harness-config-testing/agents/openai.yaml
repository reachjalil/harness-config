interface:
  display_name: "Harness config Testing"
  short_description: "Design Harness config regression coverage"
  default_prompt: "Use $harness-config-testing to add focused Harness config regression tests for a projection edge case."
